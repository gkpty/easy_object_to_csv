var objectToCsv = require('./csvToObject');
var csvToObject = require('./csvToObject');
var objectToDynamoDb = require('./csvToObject');
var objectToCsv = require('./csvToObject');


var dynamoDbtoCsv = {
  //dynamoDb to object
  //then object to csv
}
var csvToDynamoDb = {
  //csv to object
  //then object to dynamo Db
}